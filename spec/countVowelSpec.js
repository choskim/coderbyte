describe("countVowel", function() {
  it("should count the total number of vowels in a string", function() {
    expect(coderbyte.countVowel("hello")).toEqual(2);
  });

  it("should count the total number of vowels in a string", function() {
    expect(coderbyte.countVowel("bookstore")).toEqual(4);
  });
})